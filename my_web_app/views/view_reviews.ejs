<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Reviews for <%= listName %></title>
</head>
<body>
    <h1>Reviews for <%= listName %></h1>
    <table>
        <thead>
            <tr>
                <th>User</th>
                <th>Review</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% reviews.forEach(review => { %>
                <tr>
                    <td><%= review.name %></td>
                    <td><%= review.text %></td>
                    <td>
                        <% if (review.userid === userId) { %>
                            <a href="/edit_review/<%= review.reviewid %>?listName=<%= listName %>">Edit</a>
                        <% } %>
                        <% if (review.userid === userId || stockListUserId === userId) { %>
                            <form method="post" action="/delete_review" style="display:inline;">
                                <input type="hidden" name="reviewID" value="<%= review.reviewid %>">
                                <input type="hidden" name="listName" value="<%= listName %>">
                                <button type="submit">Delete</button>
                            </form>
                        <% } %>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
    <a href="/view_all_stock_lists">Back to Stock Lists</a>
</body>
</html>